on: push
name: 🚀 Deploy Bvoat website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    timeout-minutes: 1000
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: 📂 Sync files mobile
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: 14.128.139.71
        local-dir: ./index.html
        server-dir: /sde_design/mobile21/
        username: bvoatofficial
        password: ftpqlqhxm2@
        protocol: ftp
        port: 21
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          **/.github*
          **/.github*/**
          **/preference*
          **/preference*/**
          *.json
          *.ini
          *.gif
          *.yml
